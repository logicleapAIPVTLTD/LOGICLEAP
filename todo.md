MSME todos - 
In BOQ 
1- component name should change from project list to BOQ generator . 
2-  Boq generator components should take two input options  “Generate from images “ and “upload Existing BOQ” 
3- onclick on “Generate from images” or when this option is on selected it should show some input like - project name , location, state & tier , project type , and take or ask for two image 1-floor/plan , floor/plan will take the input of how much work is needed (dimension, areas) , 2-2D Layout/Design drawing, (what work needs to be done (specifications, materials) , then it generates BOQ (should have a button “Generate Boq from images) 
4- and onclick on “upload existing boq”  it will show the same input like project name , location, state & tier , project type , but the image input  should change to “Upload existing boq “  ( it will extract from the excel or pre-existing Boq document) then it generates BOQ (should have a button) 
5- when boq is generated, it will show the result ,ai will give many works (8-10 or many whichever needed)  after that the customer should have a option to remove works in boq itself, then the rest and final boq will show and wbs will generate according to this final boq , 
6- work of WBS remain also same as before , just need to change or update the UI of WBS like it will show the BOQ input default (like before) and generate WBS according to that  input , 
7- BOM generator generator is like before , according to BOQ it will generate BOM and should show backend data in response, for better understanding read PRD.md